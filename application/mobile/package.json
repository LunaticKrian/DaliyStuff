{
  "name": "pixelpack-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "PixelPack 移动端（Tauri 2 · iOS/Android 瘦客户端，服务端零改动）",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "ios:dev": "tauri ios dev",
    "ios:build": "tauri ios build",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}

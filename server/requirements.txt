fastapi
uvicorn[standard]
sqlalchemy[asyncio]
alembic
asyncmy              # 应用运行期 async MySQL 驱动（C 扩展，优先 wheel）
pymysql              # Alembic 迁移用（sync，纯 Python 零编译）
pydantic-settings
email-validator
python-jose[cryptography]
cryptography            # 用户 API Key 加密（Fernet）；jose 已间接依赖，此处显式声明
passlib[bcrypt]
python-multipart
Pillow
# ── AI 资讯（世界地图）──
claude-agent-sdk
apscheduler
feedparser
html2text
httpx
# ── 测试 ──
pytest
pytest-asyncio
